export type LoginType = {
  email: string;
  password: string;
};

export type RegisterType = {
  email: string;
  password: string;
  confirmPassword: string;
};

export type ProfileType = {
  firstName: string;
  lastName: string;
  email: string;
};
