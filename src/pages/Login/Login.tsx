import { LogoText } from "../../svg";

const Login = () => {
  return (
    <main className="w-full h-full bg-light-grey p-8">
      <LogoText />
      <form className="w-full mt-[72px]"></form>
    </main>
  );
};

export default Login;
